# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際のガイドラインです。

## プロジェクト概要

(プロジェクトの内容が固まり次第、ここに概要を記載してください)

## Git 運用ルール

- **コードに変更を加えるたびに、コミットしてGitHubへプッシュすること。** 変更をローカルに溜め込まず、意味のある単位（1つの修正・1つの機能追加など）ごとに commit → push まで完了させる。
- コミットメッセージは変更内容が分かるように簡潔に書く（「何を」より「なぜ」を意識する）。
- push 先のリモート・ブランチが未設定の場合は、作業を進める前にユーザーに確認する。
- force push (`--force`) や履歴を書き換える操作は、ユーザーの明示的な許可がない限り行わない。
- 機密情報 (.env、認証情報など) を誤ってコミット・プッシュしないよう、`git status` / `git diff` で差分を確認してからコミットする。

## 開発コマンド

(ビルド・テスト・Lint などのコマンドが決まり次第、ここに追記してください)
